apiVersion: v1
kind: Service
metadata:
  name: {{ include "naive-nginx.fullname" . }}
  labels:
    {{- include "naive-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "naive-nginx.selectorLabels" . | nindent 4 }}
